import 'package:flutter/material.dart';

class USerDepartmentDetailPage extends StatelessWidget {
  const USerDepartmentDetailPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}